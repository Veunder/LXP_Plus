# Скопируй этот файл в .env и подставь свои данные.
# .env НЕ коммитить в git (он в .gitignore).

# Строка подключения к PostgreSQL:
# postgresql://пользователь:пароль@хост:порт/имя_базы
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/laptops_db
